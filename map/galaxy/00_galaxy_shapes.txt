arena = {
	core_radius_perc = 0.0				# Core radius is 0% of the galaxy radius
	num_stars_core_perc = 0				# Number of stars in core
	stars_min_dist = 10.0				# Min distance between stars

	countries = {
		ideal_sq_dist_between = 0	# no distance between countries
		min_sq_dist_between = 0		# no distance between countries
	}
}